plugins {
    id("essentials.module-conventions")
}

dependencies {
    compileOnly project(':EssentialsX')
}

jar {
    archiveFileName = "EssentialsXSpawn-OG-${rootProject.ext.FULL_VERSION}.jar"
    destinationDirectory = file("${rootProject.buildDir}/libs")
}
